<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <httpErrors existingResponse="PassThrough"/>

    <httpProtocol>
      <customHeaders>
        <add name="Connection" value="keep-alive"/>
      </customHeaders>
    </httpProtocol>

    <security>
      <requestFiltering>
        <verbs>
          <add verb="OPTIONS" allowed="true"/>
        </verbs>
      </requestFiltering>
    </security>

    <tracing>
      <traceFailedRequests>
        <clear/>
      </traceFailedRequests>
    </tracing>

    <staticContent>
      <!-- Vite-hashed assets: cache forever (filename changes on rebuild) -->
      <clientCache cacheControlMode="UseMaxAge" cacheControlMaxAge="365.00:00:00"/>
      <mimeMap fileExtension=".webmanifest" mimeType="application/manifest+json"/>
    </staticContent>

    <handlers>
      <add name="iisnode" path="src/server.js" verb="*" modules="iisnode"/>
    </handlers>

    <rewrite>
      <rules>
        <!-- 1. iisnode debug inspector — leave untouched -->
        <rule name="NodeInspector" patternSyntax="ECMAScript" stopProcessing="true">
          <match url="^src/server\.js\/debug[\/]?"/>
        </rule>

        <!-- 2. OPTIONS preflight — always to Node (CORS headers live there) -->
        <rule name="OPTIONS preflight" stopProcessing="true">
          <match url="(.*)"/>
          <conditions>
            <add input="{REQUEST_METHOD}" pattern="^OPTIONS$"/>
          </conditions>
          <action type="Rewrite" url="src/server.js"/>
        </rule>

        <!-- 3. Static assets — rewrite /assets/* and /downloads/* to public/
             only when the file physically exists; missing files fall through to Node. -->
        <rule name="Static assets" stopProcessing="true">
          <match url="^(assets|downloads)/(.*)"/>
          <conditions>
            <add input="{APPL_PHYSICAL_PATH}public\{R:1}\{R:2}" matchType="IsFile"/>
          </conditions>
          <action type="Rewrite" url="public/{R:0}"/>
        </rule>

        <!-- 4. Root-level static files (favicon.ico, etc.) in public/ -->
        <rule name="Root static files" stopProcessing="true">
          <match url="^[^/]+\.[a-zA-Z0-9]+$"/>
          <conditions>
            <add input="{APPL_PHYSICAL_PATH}public\{R:0}" matchType="IsFile"/>
          </conditions>
          <action type="Rewrite" url="public/{R:0}"/>
        </rule>

        <!-- 5. All API, receipt, and health routes → Node -->
        <rule name="Node.js API" stopProcessing="true">
          <match url="^(api|receipt|health)(/.*)?$"/>
          <action type="Rewrite" url="src/server.js"/>
        </rule>

        <!-- 6. Everything else (SPA routes + root /) → Node → serves index.html -->
        <rule name="React SPA" stopProcessing="true">
          <match url="(.*)"/>
          <action type="Rewrite" url="src/server.js"/>
        </rule>
      </rules>
    </rewrite>

    <iisnode
      devErrorsEnabled="true"
      debuggingEnabled="false"
      loggingEnabled="true"
      logDirectory="iisnode_logs"
      maxLogFileSizeInKB="10240"
      maxTotalLogFileSizeInKB="102400"
      maxLogFiles="20"
      nodeProcessCommandLine="C:\Program Files\nodejs\node.exe"
      nodeProcessCountPerApplication="1"
      maxConcurrentRequestsPerProcess="1024"
      maxNamedPipeConnectionRetry="200"
      namedPipeConnectionRetryDelay="250"
      maxNamedPipeConnectionPoolSize="512"
      maxNamedPipePooledConnectionAge="60000"
      asyncCompletionThreadCount="0"
      initialRequestBufferSize="4096"
      maxRequestBufferSize="65536"
      watchedFiles="src\*.js"
      recycleSignalEnabled="false"
    />

    <directoryBrowse enabled="false"/>
  </system.webServer>

  <system.web>
    <compilation tempDirectory="C:\Inetpub\vhosts\vengurlatech.com\tmp"/>
  </system.web>
</configuration>
