# Backend API base URL for account deletion form
# In development, the Vite dev server proxies /api → backend, so leave this blank.
# In production, set this to your backend's public URL, e.g.:
#   VITE_API_URL=https://api.VBill.in
VITE_API_URL=
